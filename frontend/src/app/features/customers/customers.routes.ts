import { Routes } from '@angular/router';
import { CustomersListComponent } from './pages/customers-list/customers-list.component';
export const CUSTOMERS_ROUTES: Routes = [{ path: '', component: CustomersListComponent }];
